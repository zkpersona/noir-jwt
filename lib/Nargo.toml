[package]
name = "lib"
type = "lib"
authors = [""]

[dependencies]
noir_hmac = { tag = "main", git = "https://github.com/zkpersona/noir-hmac", directory = "lib" }
noir_base64 = { tag = "v0.4.0", git = "https://github.com/noir-lang/noir_base64" }
